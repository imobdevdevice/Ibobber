<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context="com.reelsonar.ibobber.ImageDisplayActivity"
                style="@style/ActivityTheme">

    <ImageButton
            android:id="@+id/imageDisplayShareButton"
            android:onClick="shareImage"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentRight="true"
            android:background="@null"
            android:scaleType="fitCenter"
            android:src="@drawable/icon_white_share" />

    <ImageView
            android:id="@+id/imageDisplayImage"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginTop="44dp"
            android:layout_marginBottom="60dp" android:layout_alignParentStart="true"/>

    <ImageButton
            android:id="@+id/imageDisplayDeleteButton"
            android:onClick="deleteImage"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_centerHorizontal="true"
            android:layout_alignParentBottom="true"
            android:background="@null"
            android:scaleType="fitCenter"
            android:src="@drawable/icon_white_delete" />

</RelativeLayout>
